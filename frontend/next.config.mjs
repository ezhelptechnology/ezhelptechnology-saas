/** @type {import('next').NextConfig} */
const nextConfig = {
  /* output: "export", -- DISABLED to allow API routes */
  /* images: { unoptimized: true }, -- DISABLED associated config */
};

export default nextConfig;
